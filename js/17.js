const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); //ParseInt() es una funcion por los parentesis y con la variable dentro del ()

console.log(numero1.toString()); //toString() es un metodo por ser una funcion unida a la variable por un "." 